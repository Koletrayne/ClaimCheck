{
  "name": "claimcheck-backend",
  "version": "0.2.0",
  "description": "Backend API for the ClaimCheck Chrome extension. Extracts factual claims, retrieves public evidence, and surfaces supporting/contradicting sources.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
